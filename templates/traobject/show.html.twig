{% extends 'base.html.twig' %}

{% block title %}Voir l'objet{% endblock %}

{% block body %}
    <h1>Objet Traoùvé</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Titre</th>
                <td>{{ traobject.label }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td><img src="{{ asset('uploads/' ~ traobject.image)  }}" alt="{{ traobject.image }}"></td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ traobject.ville }}</td>
            </tr>
            <tr>
                <th>Descritption</th>
                <td>{{ traobject.descritption }}</td>
            </tr>
            <tr>
                <th>EventAt</th>
                <td>{{ traobject.eventAt ? traobject.eventAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DateEnd</th>
                <td>{{ traobject.dateEnd ? traobject.dateEnd|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ traobject.adress }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ traobject.createdAt }}</td>
            </tr>
            <tr>
                <th>UpdateAt</th>
                <td>{{ traobject.updateAt }}</td>
            </tr>
        </tbody>
    </table>


    <a class="btn" href="{{ path('homepage') }}">back to list</a>

    <a class="btn" href="{{ path('traobject_edit', {'id': traobject.id}) }}">edit</a>

    {{ include('traobject/_delete_form.html.twig') }}
{% endblock %}
