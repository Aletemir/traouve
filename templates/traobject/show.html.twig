{% extends 'base.html.twig' %}

{% block title %}Traobject{% endblock %}

{% block body %}
    <h1>Traobject</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ traobject.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ traobject.label }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ traobject.image }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ traobject.ville }}</td>
            </tr>
            <tr>
                <th>Descritption</th>
                <td>{{ traobject.descritption }}</td>
            </tr>
            <tr>
                <th>EventAt</th>
                <td>{{ traobject.eventAt ? traobject.eventAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DateEnd</th>
                <td>{{ traobject.dateEnd ? traobject.dateEnd|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ traobject.adress }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ traobject.createdAt }}</td>
            </tr>
            <tr>
                <th>UpdateAt</th>
                <td>{{ traobject.updateAt }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('traobject_index') }}">back to list</a>

    <a href="{{ path('traobject_edit', {'id': traobject.id}) }}">edit</a>

    {{ include('traobject/_delete_form.html.twig') }}
{% endblock %}
